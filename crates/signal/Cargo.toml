[package]
name = "eio-signal"
description = "CBOR value, signal, and batch types for eieio (ABI-SPEC §6.3)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
minicbor.workspace = true

[dev-dependencies]
proptest.workspace = true
# The `expr-tests/cbor/` vectors are JSON (ABI §6.3.1), read by
# `tests/cbor_vectors.rs`. Dev-only, so `check-nostd` never sees them — the same
# reason proptest above is not a problem for a ★ crate.
serde.workspace = true
serde_json.workspace = true

[lints]
workspace = true
